# Please see https://i3wm.org/docs/userguide.html for a complete reference!  
set $mod Mod4 
set $ws1 "1:abc"
set $ws10 "10:dota2"
set $ws2 "2:dfat"
set $ws3 "3:co"
set $ws4 "4:web"
set $ws5 "5:mus"
set $ws6 "6:tbc"
set $ws7 "7:ste"
set $ws8 "8:wee" 
set $ws9 "9:df"
set $MO "mons --primary HDMI2 -e left"
bindsym $mod+F7 exec $MO
exec $MO

#exec st clock-tty; move to workspace $ws2
#exec st "dfat"; move to workspace $ws2
exec --no-startup-id pasystray
exec --no-startup-id fcitx
exec --no-startup-id keep 
exec --no-startup-id copyq

exec --no-startup-id feh ~/dunleds/* --bg-fill --no-xinerama


# font pango:ProFontWindows Nerd Font Complete 12:
# font pango:mplus Nerd Font Mono 13:Envy Code R 13
# font pango:Envy Code R 14
# font pango:DejaVu Sans Mono 8
# font pango:Roboto Sans Mono 12:
#font pango:mononoki Nerd Font:Awesome  
font pango:mononoki Nerd Font Mono 10: Awesome
# font font pango:hack 12:mplus Nerd Font Mono:
# font pango:LiterationMono Nerd Font 13:

gaps inner 4 
gaps outer 1
exec --no-startup-id chromium
floating_modifier $mod

# start a terminal
#bindsym $mod+Return exec --no-startup-id termite
bindsym $mod+Return exec --no-startup-id i3-sensible-terminal
bindsym $mod+Shift+Return exec --no-startup-id termite 
bindsym $mod+Shift+q kill
bindsym $mod+q exec --no-startup-id rofi -show drun
# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right


# split in vertical orientation
bindsym $mod+z split v
bindsym $mod+u split h

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (termiteacked, tabbed, toggle split)
bindsym $mod+w layout toggle all
#bindsym $mod+s layout termiteacked
#bindsym $mod+e layout tabbed 
#bindsym $mod+Ctrl+u 
# toggle tiling / floating enable
bindsym $mod+Shift+f floating toggle 

# change focus between tiling / floating enable windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+d focus child


# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

#mons
workspace $ws1 output eDP1 
workspace $ws2 output eDP1
workspace $ws3 output HDMI2
workspace $ws9 output HDMI2
workspace $ws5 output HDMI2
workspace $ws6 output HDMI2
workspace $ws7 output HDMI2
workspace $ws8 output HDMI2
workspace $ws4 output HDMI2
workspace $ws10 output HDMI2
assign [class="^tmux$"] $ws8
assign [class="^termite"] $ws3
assign [class="^skype$"] $ws1
assign [class="^dota2$"] $ws10
assign [class="^Steam$"] $ws7
assign [class="^lnp$"] $ws9
assign [class="^Dwarf_Fortress$"] $ws9
assign [class="^dfhack$"] $ws9
assign [class="^Chromium$"] $ws4
assign [class="^weechat$"] $ws8
assign [class="^Announcement Window+$"] "2:dfat" 
assign [class="^i3-nagbar$^"]"3:co" 

#assign.2
for_window [window_role="pop-up"] floating enable move output primary
for_window [window_role="bubble"] floating enable move output primary
for_window [window_role="task_dialog"] floating enable move output primary
for_window [window_role="^copyq$"] floating enable move output primary
for_window [window_role="^Preferences$"] floating enable move output primary
for_window [window_type="^dialog$"] floating enable move output primary
for_window [window_type="^menu$"] floating enable move output primary


bindsym $mod+Ctrl+Shift+u exec --no-startup-id thunderbird
bindsym $mod+p exec --no-startup-id chromium
bindsym $mod+Ctrl+p exec --no-startup-id firefox-nightly 
bindsym $mod+Shift+d exec --no-startup-id steam-runtime steam://rungameid/373300 
bindsym $mod+Shift+i exec --no-startup-id st -e bash i -c dfhack
# reload the configuration file
bindsym $mod+Shift+c reload
# retermiteart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
hide_edge_borders active 
for_window [class=",*"] title_format "<b>%instance-%class</b>"
default_border pixel
default_floating_border pixel
exec copyq 

workspace back_and_forth output HDMI2
bindsym $mod+Tab workspace back_and_forth
bindsym $mod+Shift+Tab move container to workspace back_and_forth
bindsym $mod+n scratchpad show
bindsym $mod+Shift+n move container to scratchpad
bindsym $mod+x [urgent=latest] focus
bindsym $mod+Shift+b move workspace to output eDP1

 
#bindsym $mod+Ctrl+k exec termite -e bash -i -c "dfat"

mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"
bar {
  status_command i3blocks
  tray_output HDMI2
  font pango:mononoki Nerd Font 9

colors {
    separator #515151
    background #2d2d2d
    statusline #cccccc
    focused_workspace #99cc99 #99cc99 #000000
    active_workspace #333333 #333333 #ffffff
    inactive_workspace #2d2d2d #2d2d2d #999999
    urgent_workspace #f2777a #f2777a #ffffff
}
}

# class                 border  backgr. text    indicator
client.focused #99cc99 #99cc99 #000000 #2d2d2d
client.focused_inactive #393939 #393939 #888888 #292d2e
client.unfocused #2d2d2d #2d2d2d #999999 #292d2e
client.urgent #2f343a #900000 #ffffff #900000



exec --no-startup-id redshift
